# Обновление IP адреса для физических устройств

## Текущий IP

Сейчас используется: `192.168.2.1`

## Как обновить IP адрес

### Способ 1: Через .env файл (рекомендуется)

Создайте файл `.env` в директории `warehouse/`:

```bash
EXPO_PUBLIC_LOCAL_IP=192.168.2.1
```

### Способ 2: Напрямую в коде

**Файл**: `warehouse/src/config/api.ts`

```typescript
const LOCAL_NETWORK_IP = '192.168.2.1'; // Замените на ваш новый IP
```

## Как узнать IP адрес компьютера

### macOS/Linux:
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### Windows:
```cmd
ipconfig
```

Ищите IPv4 адрес в формате `10.x.x.x` или `192.168.x.x`

## Проверка

После обновления перезапустите приложение:
```bash
cd warehouse
npm start
```

В консоли вы увидите:
```
🌐 API Configuration:
   Environment: development
   Device Type: Physical Device  # или Emulator/Simulator
   Base URL: http://10.220.73.143:3000
   Platform: android
```

## Важно

- Убедитесь что компьютер и телефон в одной WiFi сети
- Проверьте что сервер запущен: `cd back_warehouse && npm run start:dev`
- Если не работает, проверьте файрвол на компьютере
